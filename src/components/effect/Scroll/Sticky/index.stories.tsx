import { ComponentMeta, ComponentStoryObj } from '@storybook/react'

import { Sticky } from '.'

export default {
  component: Sticky,
} as ComponentMeta<typeof Sticky>

export const Default: ComponentStoryObj<typeof Sticky> = {}
